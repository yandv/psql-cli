#!/usr/bin/env node
import('../dist/cli.js').catch((err) => {
  if (err && err.code === 'ERR_MODULE_NOT_FOUND') {
    console.error('psql-cli: build missing. Run "npm run build" in the project directory.');
    process.exit(1);
  }
  console.error(err);
  process.exit(1);
});
